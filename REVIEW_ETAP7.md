### 🔍 РЕВЬЮ

**Задача:** Этап 7 — Бот: ежедневные напоминания, уведомления о рейтинге и streak

**Вердикт:** ✅ Принято

**Общая оценка:** Чистая реализация. Три BullMQ-джоба с cron-расписанием, три internal API эндпоинта, защищённые `X-Internal-Secret`. Код следует установленным паттернам проекта. Все ошибки при отправке сообщений и вызовах API обрабатываются gracefully. MVP-ограничения (rank-drops заглушка, нет opt-out) документированы и обоснованы.

### 🚨 КРИТИЧНЫЕ ЗАМЕЧАНИЯ (блокируют приёмку)

Критичных замечаний нет.

### 💡 РЕКОМЕНДАЦИИ (не блокируют, но желательно)

| # | Файл | Строка/место | Предложение | Обоснование |
|---|------|--------------|-------------|-------------|
| 1 | `packages/api/src/routes/notifications.ts` | `checkSecret`, строки 8–15 | Заменить ручную функцию `checkSecret` с грязными `as never` кастами на Fastify preHandler (по аналогии с существующим `authMiddleware`). Можно вынести `internalAuthMiddleware` в общий модуль и использовать в `subscription.ts` и `notifications.ts`. | Убирает дублирование (проверка секрета в subscription.ts и здесь), убирает `as never`, консистентно с остальным API. |
| 2 | Все три job-файла | Конфигурация `connection` | `REDIS_URL`, `API_HOST`, `API_PORT`, `INTERNAL_SECRET`, `WEBAPP_URL` дублируются во всех трёх файлах. | Вынести в общий `packages/bot/src/config.ts` для единого источника конфигурации. |
| 3 | `packages/bot/src/jobs/daily-reminder.ts` | Worker callback, цикл отправки | Последовательная отправка сообщений 500 пользователям может занять значительное время. | Для масштабирования — параллельная отправка батчами (например, `Promise.allSettled` по 25–30 сообщений с паузой между батчами для соблюдения лимитов Telegram API). |
| 4 | `packages/api/src/routes/notifications.ts` | `GET /api/internal/inactive-users`, SQL-запрос | `NOT IN (SELECT DISTINCT ...)` на большой таблице `task_attempts` может быть медленным. | При росте базы — заменить на `LEFT JOIN ... WHERE attempt IS NULL` или добавить поле `lastActiveAt` в таблицу `users`. |

### 📊 ОЦЕНКА ПО КРИТЕРИЯМ

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Корректность | ✅ | SQL-запросы корректны, cron-расписания верны, graceful error handling |
| Обратная совместимость | ✅ | Только новые модули, существующий функционал не затронут |
| Архитектура | ✅ | BullMQ + internal API — правильный подход для bot↔API взаимодействия с фоновыми задачами |
| Поддерживаемость | ⚠️ | Дублирование конфигурации, `as never` касты |
| Простота | ✅ | Минимальный необходимый код, без переусложнений |

### 🔄 ПРОВЕРКА СООТВЕТСТВИЯ ПЛАНУ

- **7.1 Ежедневные напоминания** — выполнено: BullMQ cron 10:00 UTC, fetch inactive users, отправка с WebApp-кнопкой
- **7.2 Уведомление о рейтинге** — инфраструктура готова, данные — MVP-заглушка (документировано, обосновано)
- **7.3 Уведомление о streak** — выполнено: cron 22:00 UTC, корректный SQL (streak > 0 AND lastActivityDate < today)
- **Internal API** — 3 эндпоинта, защищены `X-Internal-Secret`
- **Интеграция** — job-ы зарегистрированы в bot, routes в API
- **Отклонения:** rank-drops заглушка и отсутствие opt-out документированы — допустимо для MVP

### ⚠️ РИСКИ РЕГРЕССИИ

- **Существующий бот** — ✅ снят: новые модули только добавлены, команды и обработчики не изменены
- **Redis обязателен для BullMQ** — подтверждён, но при недоступности Redis бот продолжит работать (worker-ы просто не запустятся). Допустимо.
- **Массовая рассылка** — LIMIT 500 защищает от перегрузки. При росте базы потребуется пагинация.

### 📦 ИТОГ

**✅ Принято:**
- Три BullMQ-джоба с корректным cron-расписанием
- Internal API с проверкой секрета, корректные SQL-запросы
- Graceful error handling (blocked bot, API unavailable)
- Существующий функционал не затронут
- Задача может быть закрыта
