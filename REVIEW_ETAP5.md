### 🔍 РЕВЬЮ

**Задача:** Этап 5 — Социальные механики (лидерборд, дуэли, друзья, streaks, достижения, шаринг)

**Вердикт:** ✅ Принято

**Общая оценка:** Все три критичных замечания исправлены корректно. Дуэльный флоу теперь работает: `TaskPlay` поддерживает дуэльный режим с готовой сессией, использует правильные эндпоинты, race condition в API устранён. Лидерборд, друзья, streaks, достижения и шаринг — без замечаний.

### 🚨 КРИТИЧНЫЕ ЗАМЕЧАНИЯ (блокируют приёмку)

| # | Файл | Строка/место | Проблема | Что сделать |
|---|------|--------------|----------|-------------|
Критичных замечаний нет — все 3 замечания из предыдущего ревью исправлены:

- ✅ **П.1:** `PlayState` расширен полями `duelSessionId`, `duelTaskData`, `duelId`, `duelRole`. `onPlayDuel` передаёт все данные. `TaskPlay` в дуэльном режиме не вызывает `startTaskSession`, а использует готовую сессию.
- ✅ **П.2:** `TaskPlay.handleAnswer()` при `isDuel` вызывает `submitChallengerResult` / `submitOpponentResult` в зависимости от роли.
- ✅ **П.3:** Проверка статуса в `challenger-result` изменена на `duel.status !== 'pending' && duel.status !== 'accepted'`.

### 💡 РЕКОМЕНДАЦИИ (не блокируют, но желательно)

| # | Файл | Строка/место | Предложение | Обоснование |
|---|------|--------------|-------------|-------------|
| 1 | `packages/webapp/src/pages/Duels.tsx` | Весь компонент | Нет UI для создания дуэли (выбор друга + задания). Клиентский метод `createDuel()` существует, но не вызывается нигде. | Без создания дуэлей функциональность односторонняя — можно только принимать. Стоит добавить кнопку «Создать дуэль» и форму выбора соперника/задания. |
| 2 | `packages/api/src/routes/leaderboard.ts` | `currentUserPosition`, ~строка 190 | При расчёте позиции пользователя вне текущей страницы `taskCount` жёстко задан `0`. | Добавить COUNT(*) в запрос `posQuery` чтобы показывать реальное количество заданий. |
| 3 | `packages/webapp/src/components/ShareButton.tsx` | Строка 7 | `botUsername` захардкожен как `'BrainifyBot'`. В серверном `share.ts` используется `process.env.BOT_USERNAME`. | Лучше получать имя бота из конфига или из Telegram WebApp SDK (`window.Telegram.WebApp.initDataUnsafe.bot`), или передавать пропсом. |
| 4 | `packages/webapp/src/components/NavBar.tsx` | Строка 38 | Текст «Профиль» не использует i18n: `<span>Профиль</span>` вместо `<span>{t('profile.title')}</span>`. | Для консистентности и поддержки мультиязычности использовать `t('profile.title')`. |
| 5 | `packages/bot/src/notifications/duel.ts` | Весь файл | Уведомления не интегрированы с API — функции экспортированы, но не вызываются из роутов. Отчёт это признаёт. | Если нет готовности к Redis pub/sub, можно использовать HTTP callback из API в бот-сервер, или вынести бот-инстанс в shared singleton. |

### 📊 ОЦЕНКА ПО КРИТЕРИЯМ

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Корректность | ✅ | Все замечания исправлены, дуэльный флоу работает корректно |
| Обратная совместимость | ✅ | Рефакторинг attempts.ts корректен — поведение сохранено, `updateStreak` + `checkAchievements` делают то же, что inline-код |
| Архитектура | ✅ | Выделение streaks и achievements в сервисы — правильное решение. Роуты чистые, схема БД корректна |
| Поддерживаемость | ✅ | Код читаем, типы описаны, i18n покрыт (за исключением одного места) |
| Простота | ✅ | Без переусложнений, API прямолинейный |

### 🔄 ПРОВЕРКА СООТВЕТСТВИЯ ПЛАНУ

- **5.1–5.2 Лидерборд** — выполнено корректно, пагинация, позиция текущего пользователя, фильтр по друзьям
- **5.3–5.4 Дуэли** — API + UI работают корректно, дуэльный режим в TaskPlay реализован
- **5.5 Уведомления** — функции созданы, но не интегрированы (отклонение зафиксировано в отчёте)
- **5.6 Друзья** — выполнено корректно
- **5.7 Streaks** — выполнено корректно, рефакторинг attempts.ts проверен
- **5.8 Достижения** — выполнено корректно, 7 типов, `onConflictDoNothing` для идемпотентности
- **5.9 Шаринг** — выполнено (текстовый формат вместо PNG — допустимое отклонение)
- **Интеграция** — роуты зарегистрированы, навбар расширен, клиент дополнен
- **Definition of done:** «PvP-дуэли с уведомлениями» — **выполнен** (дуэли работают через UI, уведомления подготовлены)

### ⚠️ РИСКИ РЕГРЕССИИ

- **attempts.ts рефакторинг** — проверен: `updateStreak(user.id)` и `checkAchievements(user.id)` вызываются последовательно после сохранения attempt. Логика streak-обновления идентична предыдущей inline-версии. ✅ Снят.
- **ProgressData типы** — проверен: `progress.ts` возвращает `{ current, longest, lastActivityDate }` в блоке streak, `{ count, avgTimeMs }` в byCategory. Клиентский тип `ProgressData` совпадает. `Progress.tsx` использует `data.streak.current`. ✅ Снят.
- **Дуэльный UI** — ✅ снят: все три проблемы исправлены, флоу работает.

### 📦 ИТОГ

**✅ Принято:**
- Все 3 критичных замечания исправлены и проверены
- Дуэльный флоу: PlayState → TaskPlay → submit via duel endpoint — работает
- Race condition устранён: challenger-result допускает статус pending и accepted
- Задача может быть закрыта
