### 🔍 РЕВЬЮ

**Задача:** Исправление Profile/Subscription — deep links, цена, i18n, обработка ошибок

**Вердикт:** ✅ Принято

**Общая оценка:** Все 5 проблем (P1–P5) исправлены корректно. Deep links обрабатываются, цена в копейках верна, i18n-ключи совпадают между кодом и JSON-файлами, ошибки отображаются в Profile UI.

### 🚨 КРИТИЧНЫЕ ЗАМЕЧАНИЯ (блокируют приёмку)

Критичных замечаний нет.

### 💡 РЕКОМЕНДАЦИИ (не блокируют, но желательно)

| # | Файл | Строка/место | Предложение | Обоснование |
|---|------|-------------|-------------|-------------|
| 1 | `packages/bot/src/payments/payments.ts` | строка 4 | `PROVIDER_TOKEN` считывается при импорте модуля. Если `PAYMENT_PROVIDER_TOKEN` задаётся позже (при запуске контейнера), значение может быть пустым. | Для consistency с другими env-переменными (которые читаются в момент использования) можно считывать `process.env.PAYMENT_PROVIDER_TOKEN` непосредственно в `sendCardInvoice`. Не критично — в Docker env задаётся до старта. |
| 2 | `packages/webapp/src/pages/Profile.tsx` | строка 51, `useEffect` | В зависимостях `useEffect` нет `t`, хотя `t` используется в `.catch()`. | При смене локали `error` не обновится. Мелочь — ошибка после смены локали маловероятна. |

### 📊 ОЦЕНКА ПО КРИТЕРИЯМ

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Корректность | ✅ | Deep links subscribe/subscribe_stars корректно маршрутизируются; цена 29900 kopecks = 299 RUB; все 14 i18n-ключей `bot.*` + 7 `achievements.*` присутствуют в ru.json и en.json |
| Обратная совместимость | ✅ | `sendCardInvoice`/`sendStarsInvoice` — новые экспорты, не ломают существующие inline-обработчики; `pre_checkout_query` и `successful_payment` handlers не затронуты |
| Архитектура | ✅ | Корректное разделение: функция invoice → вызывается из команды и из deep link; i18n через shared-пакет |
| Поддерживаемость | ✅ | i18n-ключи структурированы по секциям, ACHIEVEMENT_META использует ключи вместо строк |
| Простота | ✅ | Изменения точечные, без переусложнений |

### 🔄 ПРОВЕРКА СООТВЕТСТВИЯ ПЛАНУ

- ✅ P1 — deep links `subscribe` / `subscribe_stars` обрабатываются в `start.ts`
- ✅ P2 — цена `29900` (kopecks). Комментарий поясняет
- ✅ P3 — achievement names в Profile через `t(meta.nameKey)`, ключи `achievements.*` в обоих JSON
- ✅ P4 — все строки в `payments.ts` / `stars.ts` через `t(locale, key)`
- ✅ P5 — error state + UI-блок с ошибкой в Profile.tsx, `console.error` для диагностики
- Отклонений от плана нет

### ⚠️ РИСКИ РЕГРЕССИИ

- **Извлечение sendCardInvoice/sendStarsInvoice** — проверено: `registerPayments` и `registerStarsPayments` по-прежнему регистрируют inline-обработчики для `pre_checkout_query` и `successful_payment`, invoice-функции вызываются из команд. Совместимость сохранена.
- **Цена 29900** — корректна для Telegram Bot Payments API (smallest currency unit для RUB = kopecks).
- **i18n-ключи** — все ключи, используемые в коде, присутствуют в ru.json и en.json. Перекрёстная проверка пройдена.

### 📦 ИТОГ

**✅ Принято:**
- Deep links для подписки работают
- Цена корректна (29900 копеек = 299 ₽)
- 21 новый i18n-ключ добавлен в оба языка, все ссылки из кода валидны
- Error handling в Profile показывает ошибку пользователю
- Задача может быть закрыта
