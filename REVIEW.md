# 🔍 РЕВЬЮ

**Задача:** Brainify — Этапы 0–4 (инфраструктура + API + задания + бот + Mini App)

**Вердикт:** ✅ Принято

**Общая оценка:** Этапы 0–4 полностью реализованы и проверены. Инфраструктура, API с серверными сессиями, 7 генераторов заданий, Telegram-бот и Mini App — всё корректно. Все критичные замечания по безопасности и корректности устранены.

---

## 🚨 КРИТИЧНЫЕ ЗАМЕЧАНИЯ (блокируют приёмку)

### Этапы 0–2 (ранее проверены)

| # | Файл | Строка/место | Проблема | Что сделать |
|---|------|--------------|----------|-------------|
| 1 | ~~`packages/api/src/routes/tasks.ts`~~ | ~~SQL-фильтр~~ | ~~—~~ | ✅ Исправлено |
| 2 | ~~`packages/api/src/routes/attempts.ts`~~ | ~~серверные сессии~~ | ~~—~~ | ✅ Исправлено |
| 3 | ~~`packages/api/src/auth/middleware.ts`~~ | ~~timing attack~~ | ~~—~~ | ✅ Исправлено |
| 4 | ~~`packages/api/src/routes/attempts.ts`~~ | ~~валидация~~ | ~~—~~ | ✅ Исправлено |

### Этапы 3–4 (новые)

| # | Файл | Строка/место | Проблема | Что сделать |
|---|------|--------------|----------|-------------|
| 5 | ~~`packages/webapp/src/components/tasks/SchulteUI.tsx`~~ | ~~строка 14–15~~ | ~~Отправлял индексы вместо значений~~ | ✅ Исправлено — `taps` (Set) для визуала, `values` (массив) для ответа |
| 6 | ~~`packages/webapp/src/`~~ | ~~`index.ts` дубликат~~ | ~~—~~ | ✅ Исправлено — `index.ts` удалён |

---

## 💡 РЕКОМЕНДАЦИИ (не блокируют, но желательно)

| # | Файл | Строка/место | Предложение | Обоснование |
|---|------|--------------|-------------|-------------|
| 1 | `packages/api/src/routes/users.ts` | строка 19–35 | Операция upsert (select → insert) не атомарна — при параллельных запросах одного пользователя возможен race condition с дублированием записи (unique constraint error). | Обернуть в try/catch с перехватом unique violation или использовать `ON CONFLICT DO NOTHING ... RETURNING` + повторный select. |
| 2 | ~~`packages/shared/src/tasks/number-series.ts`~~ | ~~ответы в data~~ | ~~—~~ | ✅ Решено через `sanitizeForClient` |
| 3 | ~~`packages/shared/src/tasks/memory-pairs.ts`~~ | ~~дедупликация~~ | ~~—~~ | ✅ Исправлено |
| 4 | ~~`docker-compose.yml`~~ | ~~комментарий~~ | ~~—~~ | ✅ Добавлено |
| 5 | ~~`.env.example`~~ | ~~LOG_LEVEL~~ | ~~—~~ | ✅ Добавлено |
| 6 | `packages/api/src/index.ts` | общее | Нет rate limiting, хотя в ARCHITECTURE.md указано «Rate limiting на API с первого дня». | Добавить `@fastify/rate-limit` хотя бы в базовом варианте. Не блокирует, но желательно до деплоя. |
| 7 | `packages/bot/src/commands/quick-task.ts` | строка 29 | `parse_mode: 'MarkdownV2'` — если в будущем `taskName` из i18n будет содержать спецсимволы MarkdownV2 (`.`, `!`, `-`, `+`, `=`), сообщение сломается. Сейчас «Арифметика» / «Arithmetic» безопасны, но при добавлении заданий с символами возникнет проблема. | Экранировать `taskName` для MarkdownV2 или использовать `parse_mode: 'HTML'`. |
| 8 | `packages/webapp/src/pages/Progress.tsx` | строка 28 | `.catch(() => {})` — ошибка загрузки прогресса молча проглатывается, пользователь видит только «Произошла ошибка» без деталей. | Логировать ошибку в `catch` или показывать retry-кнопку. |
| 9 | `packages/webapp/src/app.tsx` | общее | `preact-router` установлен в зависимостях, но не используется — вместо него state-based роутинг. | Либо убрать `preact-router` из `package.json`, либо использовать при добавлении новых страниц. На данном этапе state-based достаточен. |
| 6 | `packages/api/src/index.ts` | строки 1–28 | Нет rate limiting, хотя в ARCHITECTURE.md указано «Rate limiting на API с первого дня». | Добавить `@fastify/rate-limit` хотя бы в базовом варианте. На данном этапе некритично, но при деплое может стать проблемой. |
| 7 | `packages/shared/src/i18n/locales/*.json` | общее | Ключи i18n содержат секции для `subscription`, `achievements`, `profile`, `settings` — но их ещё нет. По мере разработки потребуется добавлять. | Ожидаемо для текущего этапа, но стоит завести задачу на дополнение ключей при реализации этапов 5–6. |

---

## 📊 ОЦЕНКА ПО КРИТЕРИЯМ

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Корректность | ✅ | Все 7 UI-компонентов отправляют данные в правильном формате. Score вычисляется на сервере через сессии. HMAC через timingSafeEqual. |
| Обратная совместимость | ✅ | Greenfield-проект. API контракт с сессиями (`POST /start` → `POST /attempts`) корректен. |
| Архитектура | ✅ | Чистое разделение: бот, API, webapp, shared. State-based роутинг — адекватен для 4 экранов. |
| Поддерживаемость | ✅ | Код читаемый, компоненты изолированы, i18n используется повсеместно. |
| Простота | ✅ | Минимум зависимостей, 12 КБ gzip бандл, CSS bar chart вместо тяжёлой библиотеки — правильный trade-off. |

---

## 🔄 ПРОВЕРКА СООТВЕТСТВИЯ ПЛАНУ

**Этап 0 (инфраструктура):** ✅ Полностью выполнен (проверено ранее)

**Этап 1 (API):** ✅ Полностью выполнен (проверено ранее)

**Этап 2 (задания):** ✅ Полностью выполнен (проверено ранее)

**Этап 3 (бот):**
- ✅ 3.1 — `/start` с deep linking (`ref_<userId>`)
- ✅ 3.2 — Кнопка-меню WebApp (`setChatMenuButton`)
- ✅ 3.3 — `/train` — быстрое задание (арифметика с inline-кнопками)
- ✅ 3.4 — `/invite` — реферальная ссылка с кнопкой «Поделиться»

**Этап 4 (Mini App):**
- ✅ 4.1 — Vite + Preact + Telegram SDK
- ✅ 4.2 — Авторизация через `initData` (`useAuth`)
- ✅ 4.3 — Каталог заданий с фильтрацией по категориям
- ⚠️ 4.4–4.5 — TaskRunner + UI-компоненты: 6 из 7 корректны, SchulteUI содержит баг формата ответа
- ✅ 4.6 — Экран результата
- ✅ 4.7 — Экран прогресса (streak + bar chart + категории)
- ✅ 4.8 — i18n (`useI18n`, Telegram locale)
- ✅ 4.9 — Оптимизация: 12 КБ gzip (бюджет < 200 КБ)

**Отклонения от плана:**
- State-based роутинг вместо `preact-router` — **обосновано** (4 экрана, code splitting добавится позже)
- CSS bar chart вместо uPlot — **обосновано** (экономия бандла)
- `preact-router` в зависимостях но не используется — **мелочь**, убрать при случае

---

## ⚠️ РИСКИ РЕГРЕССИИ

- **Подтверждённые риски — сняты:**
  - ~~SchulteUI баг~~ — ✅ исправлено
  - ~~Дубликат `index.ts`~~ — ✅ удалён

- **Риски от Исполнителя — проверены:**
  - «Бот требует BOT_TOKEN» — подтверждено, throw при отсутствии — корректное поведение.
  - «Mini App работает только в Telegram WebView» — подтверждено, `getInitData()` возвращает пустую строку вне Telegram, API отдаст 401. Dev proxy покрывает разработку.
  - «Существующие тесты не затронуты» — подтверждено (19/19 проходят по отчёту).

---

## 📦 ИТОГ

**✅ Принято**

Все 6 критичных замечаний (4 из этапов 0–2, 2 из этапов 3–4) устранены:
- ✅ 1 — SQL-фильтр + валидация category
- ✅ 2 — Серверные сессии задач (`task_sessions` + `sanitizeForClient`)
- ✅ 3 — HMAC timing attack (`timingSafeEqual`)
- ✅ 4 — Валидация входных данных
- ✅ 5 — SchulteUI: индексы → значения
- ✅ 6 — Дубликат `index.ts` удалён

Задача может быть закрыта. Следующие шаги по плану: Этап 5 (социальные механики).
